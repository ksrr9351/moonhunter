services:
  backend:
    build: ./backend
    container_name: moonhunters-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - ENV=production
